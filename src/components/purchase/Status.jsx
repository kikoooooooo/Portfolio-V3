const comStat = x;
// o = open, w = working, c = closed; update comStat
switch (comStat) {
    case o:
        document.write();
        break;
    case w:
        break;
    case x:
        break;
    default:
        //working
}